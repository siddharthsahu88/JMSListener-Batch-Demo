package com.jmslistener.demo.component;

import org.springframework.stereotype.Component;

@Component
public class JMSListenerComp {

	/*
	 * @JmsListener(concurrency = "1", destination = "${activemq.queue}") public
	 * void receiveMessage(String msg) { System.out.println(msg); }
	 */

}
